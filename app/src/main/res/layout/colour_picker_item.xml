<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <!-- The "colour block" representing the category colours -->
    <View
        android:id="@+id/colorItem"
        android:layout_height="48dp"
        android:layout_width="48dp"
        android:padding="4dp"
        android:background="@drawable/colour_square"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" >
    </View>

    <!-- The checkmark icon sitting on top of the block -->
    <!-- This is hidden by default and displayed when clicked -->
    <View
        android:id="@+id/checkmark"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:padding="4dp"
        android:background="@drawable/checkmark"
        android:visibility="gone"
        app:layout_constraintDimensionRatio="1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" >

    </View>

</androidx.constraintlayout.widget.ConstraintLayout>